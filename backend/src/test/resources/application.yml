spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/gym_app
    username: gym_app
    password: gym_app
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true

gym-app:
  auth:
    jwt-secret: ${GYM_JWT_SECRET}